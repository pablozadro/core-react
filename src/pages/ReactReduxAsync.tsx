import Section from '../elements/section/Section';
import Users from '../components/users/Users';

export default function ReactReduxAsync () {
  return (
    <div className="container">
      <Section title="Redux Async">
        <Users />
      </Section>
    </div>
  )
};